/**
 * diz-se que uma linguagem de programação tem
 * Função de primeira classe quando funçoes nessa
 * linguagem são tratadas como qualquer outra
 * variável
 */

const add = function (a, b) {
    return a + b
}

console.log(add(10, 20));